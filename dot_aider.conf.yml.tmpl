anthropic-api-key: {{ (join .chezmoi.sourceDir ".encrypted_keys.toml.asc" | include | decrypt | fromToml).keys.anthropic_api | quote }}
api-key:
- deepseek={{ (join .chezmoi.sourceDir ".encrypted_keys.toml.asc" | include | decrypt | fromToml).keys.deepseek_api }}
- gemini={{ (join .chezmoi.sourceDir ".encrypted_keys.toml.asc" | include | decrypt | fromToml).keys.gemini_api }}

model: gemini/gemini-2.5-pro-preview-05-06

read:
  - CONVENTIONS.md
